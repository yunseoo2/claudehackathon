# Database connection string for SQLAlchemy
# Use this exact value when running Postgres via docker-compose in this repo
# DATABASE_URL=postgresql+psycopg2://continuum:continuum@localhost:5555/continuum

# Anthropic API Key for Claude integration
# Get your API key from: https://console.anthropic.com/
# Leave empty to use mocked responses during development
ANTHROPIC_API_KEY=
